export type dataTypes = {
  ss58Format: number;
  tokenDecimals: number[];
  tokenSymbols: string[];
  connected: boolean;
  icon: string;
  name: string;
  node: string;
  paraId: number;
  relayChain: string;
};
